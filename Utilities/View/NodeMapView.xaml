<UserControl x:Class="Utilities.View.NodeMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Utilities.View"
             xmlns:viewModels="clr-namespace:Utilities.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1000">
    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
        <Canvas x:Name="_canvas" ClipToBounds="True">
            <Rectangle 
                Width="{Binding ActualWidth, ElementName=_canvas}" 
                Height="{Binding ActualHeight, ElementName=_canvas}"
                Fill="LightBlue"
                Panel.ZIndex="-50" />
        </Canvas>
    </Border>
</UserControl>
